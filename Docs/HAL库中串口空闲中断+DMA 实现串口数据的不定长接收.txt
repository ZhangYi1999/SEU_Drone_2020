HAL库中串口空闲中断+DMA 实现串口数据的不定长接收

不要加memset(receive_buff,0,data_length); 
这个会让整个buffer变零，如果在此函数之前把数据处理完了那确实是应该加的，但是如果加了之后，调试会发现buffer里面所有的数据都是零，你会懵逼很长时间以为自己搞错了，但是实际上没有，以为处理完数据之后又给置零了，所以不如不加，然后在数据处理函数中要加上counter来记录不定长数据的大小